# Plantilla del .env de PRODUCCIÓN. Copia este archivo como /opt/slotify/.env en el VPS
# y rellena los valores. NO lo subas al repo (los valores reales son secretos).
# Genera los secretos con los comandos de docs/DEPLOY.md.

# Dominio (para Caddy + CORS)
DOMAIN=slotify.jjalarcon.es

# PostgreSQL (la BD vive solo dentro de Docker)
POSTGRES_DB=slotify
POSTGRES_USER=slotify_user
POSTGRES_PASSWORD=CAMBIA_ESTO_password_largo

# Clave de firma JWT (mínimo 32 bytes). Genera: openssl rand -base64 48
JWT_KEY=CAMBIA_ESTO_clave_jwt

# Claves de cifrado del contacto del invitado (AES-256-GCM + blind index).
# Cada una: 32 bytes en base64. Genera: openssl rand -base64 32
CRYPTO_ENCRYPTION_KEY=CAMBIA_ESTO_base64_32_bytes
CRYPTO_BLIND_INDEX_KEY=CAMBIA_ESTO_base64_32_bytes

# Email real por SMTP (IONOS, STARTTLS). Si se dejan vacías o se omiten,
# el backend simula el envío por log (modo TFM/desarrollo).
SMTP_HOST=smtp.ionos.es
SMTP_PORT=587
SMTP_USER=slotify@jjalarcon.es
SMTP_PASSWORD=CAMBIA_ESTO_password_buzon

# (Opcional) buzón que recibe el formulario de contacto; por defecto slotify@jjalarcon.es
# SUPPORT_RECIPIENT_EMAIL=slotify@jjalarcon.es

# WhatsApp real vía Twilio (sandbox en desarrollo: From +14155238886 y los
# destinatarios se unen con el código del sandbox). Si se omiten, los avisos
# de WhatsApp se simulan por log.
# TWILIO_ACCOUNT_SID=ACxxxxxxxxxxxxxxxx
# TWILIO_AUTH_TOKEN=CAMBIA_ESTO_token
# TWILIO_WHATSAPP_FROM=+14155238886

# (Opcional) admin de plataforma: la cuenta registrada con este email puede moderar
# (listar/eliminar negocios) desde /admin. Sin configurar, no hay admin.
# ADMIN_EMAIL=josejua94@gmail.com

# (Opcional) pago Premium con Stripe: claves del dashboard (modo test: sk_test_…).
# El webhook debe apuntar a https://DOMINIO/api/webhooks/stripe (evento
# checkout.session.completed). Sin claves, el checkout es simulado (demo).
# STRIPE_SECRET_KEY=sk_test_CAMBIA_ESTO
# STRIPE_PRICE_ID=price_CAMBIA_ESTO
# STRIPE_WEBHOOK_SECRET=whsec_CAMBIA_ESTO

# (Opcional) etiqueta de imagen a desplegar; por defecto 'latest'.
# IMAGE_TAG=latest

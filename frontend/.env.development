# Target del backend para el proxy del dev-server de Vite (ver vite.config.ts).
# El navegador SIEMPRE llama a rutas relativas /api/... y Vite las reenvía aquí,
# así no hay problemas de CORS en desarrollo (mismo origen que el front).
VITE_API_URL=http://localhost:5000
